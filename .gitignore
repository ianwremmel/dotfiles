# the vim bundle directory is populated during the apply process
**/home/.vim/bundle/

custom_environments

.claude/settings.local.json
.claude/worktrees/

# JetBrains IDE settings (machine-local)
.idea/
